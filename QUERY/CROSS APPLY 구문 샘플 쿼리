SELECT SUBSTRING(MODEL_NO, startIdx + 1, endIdx - startIdx - 1) AS RESULT
FROM LEADER_PRODUCT_IF
CROSS APPLY (SELECT CHARINDEX('-', MODEL_NO)) AS start(startIdx)
CROSS APPLY (SELECT CHARINDEX('-', MODEL_NO, startIdx + 1)) AS [end](endIdx)
